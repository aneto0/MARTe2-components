Some of the tests require a softIoc to be running:
Make sure to execute: softIoc -d MARTeEPICSCATest.db before executing the tests.
The softIoc should also be restarted before running the tests, in order to guarantee that the memory is clean. 